name: Release

# 推送形如 v0.1.23 的 tag 时触发
on:
  push:
    tags:
      - 'v*'

jobs:
  build-and-release:
    runs-on: macos-15        # Apple Silicon runner

    steps:
      # ── 1. Checkout ──────────────────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v4

      # ── 2. 从 tag 提取版本号（去掉前缀 v）────────────────────────
      - name: Extract version
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      # ── 3. 导入 Developer ID 证书到临时钥匙串 ────────────────────
      - name: Import certificate
        env:
          CERTIFICATE_BASE64: ${{ secrets.MACOS_CERTIFICATE }}
          CERTIFICATE_PWD:    ${{ secrets.MACOS_CERTIFICATE_PWD }}
        run: |
          KEYCHAIN_PWD=$(openssl rand -hex 16)
          KEYCHAIN_PATH="$RUNNER_TEMP/build.keychain"
          CERT_PATH="$RUNNER_TEMP/certificate.p12"

          echo "$CERTIFICATE_BASE64" | base64 --decode -o "$CERT_PATH"
          security create-keychain -p "$KEYCHAIN_PWD" "$KEYCHAIN_PATH"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_PATH"
          security unlock-keychain -p "$KEYCHAIN_PWD" "$KEYCHAIN_PATH"
          security import "$CERT_PATH" -k "$KEYCHAIN_PATH" \
            -P "$CERTIFICATE_PWD" -T /usr/bin/codesign -T /usr/bin/security
          security list-keychain -d user -s "$KEYCHAIN_PATH"
          security set-key-partition-list -S apple-tool:,apple: \
            -s -k "$KEYCHAIN_PWD" "$KEYCHAIN_PATH"

      # ── 4. 存储公证凭据到钥匙串 ──────────────────────────────────
      - name: Store notarytool credentials
        env:
          APPLE_ID:       ${{ secrets.APPLE_ID }}
          APPLE_TEAM_ID:  ${{ secrets.APPLE_TEAM_ID }}
          APPLE_APP_PWD:  ${{ secrets.APPLE_APP_PASSWORD }}
        run: |
          xcrun notarytool store-credentials "AC_NOTARY" \
            --apple-id    "$APPLE_ID" \
            --team-id     "$APPLE_TEAM_ID" \
            --password    "$APPLE_APP_PWD"

      # ── 5. 构建 + 签名 + 公证 + 打包 ─────────────────────────────
      - name: Build, sign & notarize
        env:
          VERSION:        ${{ steps.version.outputs.VERSION }}
          SIGN_ID:        ${{ secrets.SIGN_ID }}
          NOTARY_PROFILE: AC_NOTARY
        run: |
          bash scripts/build_release.sh

      # ── 6. 发布到公开 releases 仓库 ──────────────────────────────
      # 源码在私有仓库，zip 发布到 peterzhangbo/AIMA-releases（公开）
      # 需在私有仓库 Secrets 中添加 RELEASE_PAT（有 public_repo 权限的 PAT）
      - name: Publish to public releases repo
        uses: softprops/action-gh-release@v2
        with:
          repository: peterzhangbo/AIMA-releases
          token: ${{ secrets.RELEASE_PAT }}
          tag_name: ${{ github.ref_name }}
          name: "AIMA v${{ steps.version.outputs.VERSION }}"
          draft: false
          prerelease: ${{ contains(github.ref_name, '-') }}
          generate_release_notes: false
          body: |
            ## 下载安装

            点击下方 **AIMA-${{ steps.version.outputs.VERSION }}.zip** 下载。

            **安装步骤**
            1. 解压得到 `AIMA.app`
            2. 拖入 `/Applications`
            3. 右键 → 打开（首次 Gatekeeper 提示时选「打开」）

            **系统要求**：Apple Silicon · macOS 14 Sonoma 及以上
          files: .build/release/AIMA-${{ steps.version.outputs.VERSION }}.zip
